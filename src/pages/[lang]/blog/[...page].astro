---
import { fetchPosts, blogPostsPerPage, blogListRobots } from '~/utils/blog';
import { LANGUAGES } from '~/i18n/utils';
import Layout from '~/layouts/PageLayout.astro';
import BlogList from '~/components/blog/List.astro';
import Headline from '~/components/blog/Headline.astro';
import Pagination from '~/components/blog/Pagination.astro';

export const prerender = true;

export const getStaticPaths = (async ({ paginate }) => {
  const locales = Object.keys(LANGUAGES);
  const allPosts = await fetchPosts();

  const paths = await Promise.all(
    locales.map(async (lang) => {
      let langPosts = allPosts.filter(p => p.lang === lang);
      let isFallback = false;

      if (langPosts.length === 0) {
        langPosts = allPosts; // Zeige alle Posts, wenn für die Sprache keine da sind
        isFallback = true;
      }

      return paginate(langPosts, {
        params: { lang },
        pageSize: blogPostsPerPage || 6,
        props: { isFallback }
      });
    })
  );

  return paths.flat();
}) satisfies GetStaticPaths;

const { page, isFallback } = Astro.props;
const { lang } = Astro.params;

const metadata = {
  title: `Blog`,
  robots: { index: !isFallback, follow: true },
};
---

<Layout metadata={metadata}>
  <section class="px-6 py-12 sm:py-16 lg:py-20 mx-auto max-w-4xl">
    {isFallback && lang === 'de' && (
      <div class="mb-8 bg-blue-50 dark:bg-slate-800 border-l-4 border-blue-500 p-4 rounded shadow-sm">
        <p class="text-sm text-blue-800 dark:text-blue-200">
          Wir haben noch keine deutschen Artikel veröffentlicht. Hier sind unsere neuesten englischen Beiträge:
        </p>
      </div>
    )}
    
    <Headline> {lang === 'de' ? 'Der Blog' : 'The Blog'} </Headline>
    <BlogList posts={page.data} />
    <Pagination prevUrl={page.url.prev} nextUrl={page.url.next} />
  </section>
</Layout>
