---
import { fetchPosts, blogPostRobots } from '~/utils/blog';
import { LANGUAGES } from '~/i18n/utils'; // Deine SSoT
import merge from 'lodash.merge';
import Layout from '~/layouts/PageLayout.astro';
import SinglePost from '~/components/blog/SinglePost.astro';
import ToBlogLink from '~/components/blog/ToBlogLink.astro';
import RelatedPosts from '~/components/blog/RelatedPosts.astro';
import Notice from '~/components/ui/Notice.astro';
import { getCanonical, getPermalink } from '~/utils/permalinks';
import { Icon } from 'astro-icon/components';

export const prerender = true;

export async function getStaticPaths() {
  const posts = await fetchPosts();
  const locales = Object.keys(LANGUAGES); // Dynamisch aus deiner Config
  const allSlugs = [...new Set(posts.map(p => p.slug))];

  return locales.flatMap((lang) => {
    return allSlugs.map((slug) => {
      let post = posts.find((p) => p.slug === slug && p.lang === lang);
      let isFallback = false;

      if (!post) {
        post = posts.find((p) => p.slug === slug); // Finde Post in irgendeiner Sprache
        isFallback = true;
      }

      if (!post) return null;

      return {
        params: { lang, slug },
        props: { post, isFallback },
      };
    }).filter(Boolean);
  });
}

const { post, isFallback } = Astro.props;
const { lang } = Astro.params;

const url = getCanonical(getPermalink(post.permalink, 'post', lang));

const metadata = merge(
  {
    title: post.title,
    description: post.excerpt,
    robots: {
      index: !isFallback && blogPostRobots?.index, // SEO: Fallbacks nicht indexieren
      follow: blogPostRobots?.follow,
    },
    openGraph: { type: 'article', image: post.image },
  },
  { ...(post?.metadata ?? {}) }
);
---

<Layout metadata={metadata}>
{isFallback && (
    <Notice title={lang === 'de' ? 'Hinweis' : 'Note'}>
      {lang === 'de'
        ? 'Dieser Artikel ist leider noch nicht auf Deutsch verfügbar. Wir zeigen dir daher die englische Version.'
        : 'This article is currently not available in your language. We are displaying the original version.'}
    </Notice>
  )}

    <SinglePost post={post} url={url}>
      <post.Content />
    </SinglePost>
    
    <ToBlogLink />
    <RelatedPosts post={post} />
</Layout>
