<script type="module">
import CookieConsent from 'https://cdn.jsdelivr.net/npm/vanilla-cookie@latest/dist/vanilla-cookie.esm.js';
import { config } from '/libs/cookieconsent-config.js';

document.addEventListener('astro:page-load', () => {
  CookieConsent.reset();
  CookieConsent.run(config);
});
</script>
