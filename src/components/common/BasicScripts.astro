---
import { UI } from 'astrowind:config';
---

<script is:inline define:vars={{ defaultTheme: UI.theme || 'system' }}>
  // Singleton Pattern: Verhindert doppelte Ausführung
  if (window.basic_script_init) return;
  window.basic_script_init = true;

  (function() {
    // Speicher für Cleanup-Funktionen (wichtig für ViewTransitions)
    let cleanupFns = [];

    // --- HELPER ---
    const toggleClass = (el, className, condition) => {
      if (condition) el.classList.add(className);
      else el.classList.remove(className);
    };

    // Robuste Header-Höhen Berechnung
    const getHeaderHeight = () => {
      const header = document.getElementById('header');
      // Prüfen ob Header sticky oder fixed ist, sonst ist offset 0 meist ok
      if (!header) return 0;
      return header.getBoundingClientRect().height;
    };

    // --- THEME LOGIK ---
    const applyTheme = (theme) => {
      const resolvedTheme = (theme === 'system') 
        ? (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light') 
        : theme;
      
      // DOM-Manipulationen immer im Animation Frame
      requestAnimationFrame(() => {
        toggleClass(document.documentElement, 'dark', resolvedTheme === 'dark');
        document.querySelectorAll('[data-aw-theme-icon]').forEach((icon) => {
          toggleClass(icon, 'hidden', icon.getAttribute('data-aw-theme-icon') !== theme);
        });
      });
    };

    // --- 1. HEADER LOGIK (SCROLL STATE) ---
    const initHeader = () => {
      const header = document.getElementById('header');
      const stickyHeader = document.querySelector('#header[data-aw-sticky-header]');
      if (!stickyHeader) return;

      let isTicking = false;

      const onScroll = () => {
        if (!isTicking) {
          window.requestAnimationFrame(() => {
            const isScrolled = window.scrollY > 10;
            // Nur anfassen, wenn sich der Status wirklich geändert hat (Performance)
            if (stickyHeader.classList.contains('scroll') !== isScrolled) {
              stickyHeader.classList.toggle('scroll', isScrolled);
            }
            isTicking = false;
          });
          isTicking = true;
        }
      };

      window.addEventListener('scroll', onScroll, { passive: true });
      
      // Initial State setzen
      if (window.scrollY > 10) stickyHeader.classList.add('scroll');

      cleanupFns.push(() => window.removeEventListener('scroll', onScroll));
    };

    // --- 2. MENU LOGIK (MOBILE) ---
    const initMenu = () => {
      const header = document.getElementById('header');
      const menuBtn = document.querySelector('[data-aw-toggle-menu]');
      const nav = header?.querySelector('nav');
      
      if (!menuBtn || !header || !nav) return;

      const toggleMenu = () => {
        menuBtn.classList.toggle('expanded');
        document.body.classList.toggle('overflow-hidden');
        header.classList.toggle('h-screen');
        header.classList.toggle('expanded');
        header.classList.toggle('bg-page');

        const icons = menuBtn.querySelectorAll('svg, [data-aw-icon]'); 
        if (icons.length > 1) icons.forEach(icon => icon.classList.toggle('hidden'));

        nav.classList.toggle('hidden');

        // Optional: Action Buttons im Header
        const actions = header.querySelector('nav + div') || header.querySelector('div > div:last-child');
        if (actions) actions.classList.toggle('hidden');
      };

      menuBtn.onclick = toggleMenu;
      window.toggleMenuInstance = toggleMenu; // Für externen Zugriff (z.B. beim Klicken eines Links)

      // Event Delegation für Dropdowns (Performance Optimierung)
      const onNavClick = (e) => {
        const trigger = e.target.closest('.dropdown > button, .dropdown > a');
        if (!trigger || window.getComputedStyle(menuBtn).display === 'none') return;

        e.preventDefault();
        const subMenu = trigger.nextElementSibling;
        if (subMenu) {
            subMenu.classList.toggle('hidden');
            trigger.classList.toggle('expanded');
        }
      };

      nav.addEventListener('click', onNavClick);
      // Cleanup ist hier wichtig bei SPA Navigation
      cleanupFns.push(() => {
          menuBtn.onclick = null;
          nav.removeEventListener('click', onNavClick);
      });
    };

    // --- 3. GLOBAL EVENTS (CLICK HANDLING) ---
    const initGlobalEvents = () => {
      const onClick = (e) => {
        // A) Theme Toggle
        const themeBtn = e.target.closest('[data-aw-toggle-color-scheme]');
        if (themeBtn) {
          e.preventDefault();
          const themes = ['system', 'light', 'dark'];
          const current = localStorage.getItem('theme') || 'system';
          const next = themes[(themes.indexOf(current) + 1) % 3];
          localStorage.setItem('theme', next);
          applyTheme(next);
          return;
        }

        // B) Smooth Scroll für Anker-Links
        const link = e.target.closest('a');
        if (link && link.hash && link.origin === window.location.origin && link.pathname === window.location.pathname) {
          const targetEl = document.querySelector(link.hash);
          if (targetEl) {
            e.preventDefault();
            
            // Menu schließen, falls offen
            if (document.getElementById('header')?.classList.contains('expanded') && window.toggleMenuInstance) {
              window.toggleMenuInstance();
            }
            
            // Berechnung der Scroll-Position
            const headerHeight = getHeaderHeight();
            const targetPos = targetEl.getBoundingClientRect().top + window.scrollY;
            
            window.scrollTo({ top: targetPos - headerHeight, behavior: 'smooth' });
            
            // URL aktualisieren ohne Sprung
            history.pushState(null, null, link.hash);
          }
        }
      };

      document.addEventListener('click', onClick, { capture: true, passive: false });
      cleanupFns.push(() => document.removeEventListener('click', onClick, { capture: true }));
    };

    // --- 4. ANIMATIONS (INTERSECTION OBSERVER) ---
    const initAnimations = () => {
      // Wählt alle Elemente aus, die Animation-Klassen haben
      const selectors = ['[class*=" intersect:"]','[class*=":intersect:"]','[class^="intersect:"]','[class="intersect"]','.intersect-once','.intersect-quarter'];
      const elements = document.querySelectorAll(selectors.join(','));
      if (elements.length === 0) return;

      const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const el = entry.target;
            // Attribut setzen, damit CSS Animation greift
            el.setAttribute('data-aw-animated', 'true');
            el.removeAttribute('no-intersect');
            if (el.classList.contains('intersect-once')) observer.unobserve(el);
          }
        });
      }, { threshold: 0.1 });

      elements.forEach(el => {
        el.setAttribute('no-intersect', '');
        observer.observe(el);
      });

      cleanupFns.push(() => observer.disconnect());
    };

    // --- 5. INITIAL HASH SCROLL FIX (RELOAD FIX) ---
    // Sorgt dafür, dass beim Neuladen (F5) mit #hash die Position stimmt
    const fixInitialHashScroll = () => {
        if (window.location.hash) {
            const target = document.querySelector(window.location.hash);
            if (target) {
                // Kurze Verzögerung, um Layout-Shift abzuwarten
                setTimeout(() => {
                    const headerHeight = getHeaderHeight();
                    const targetPos = target.getBoundingClientRect().top + window.scrollY;
                    
                    window.scrollTo({ 
                        top: targetPos - headerHeight, 
                        behavior: 'instant' // Wichtig: Instant beim Laden, nicht smooth
                    });
                }, 10);
            }
        }
    };

    // --- MAIN LIFECYCLE ---
    const run = () => {
      // Alte Listener aufräumen
      cleanupFns.forEach(fn => fn());
      cleanupFns = [];

      // Theme anwenden
      applyTheme(localStorage.getItem('theme') || defaultTheme);
      
      // Module starten
      initHeader();
      initMenu();

      // Events verzögern um Main Thread beim Laden zu entlasten
      setTimeout(() => {
          initGlobalEvents();
          fixInitialHashScroll(); // <-- Hier wird der Reload-Position-Fix aufgerufen
      }, 0);

      // Animationen via IdleCallback laden (Performance!)
      if (window.requestIdleCallback) window.requestIdleCallback(initAnimations);
      else setTimeout(initAnimations, 200);
    };

    // Start
    run();
    
    // Astro View Transitions Hook
    document.addEventListener('astro:after-swap', run);
  })();
</script>
